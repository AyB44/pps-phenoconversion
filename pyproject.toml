[project]
name = "pps-phenoconversion"
version = "0.1.0"
description = "Reference implementation of the PPS phenoconversion framework"
requires-python = ">=3.9"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["pps"]